# CLAUDE.md — Proxmox ホームラボ運用ルール (Node0)

このファイルは Claude Code が Node0 (Proxmox VE ホスト) 上で作業する際の行動規範を定める。
**ここはハイパーバイザーである。誤操作は全VM・全データ・管理経路の喪失に直結する。**

---

## 0. 環境の前提知識

| 項目 | 内容 |
|---|---|
| ホスト | Node0: Ryzen 2600 / RAM 48GB / GTX1650 / ASRock B450M Pro4 |
| OS | Proxmox VE (NVMe 256GB, ZFS rpool) |
| ストレージ | SATA SSD 256GB = VE1用VMディスク / 内蔵6TB HDD = パススルー予定(録画・写真の実体データ) |
| VM計画 | VE1: Frigate+Immich / VE2: TrueNAS / VE3: Win11 / VE4: Pi-hole LXC / VE5: 開発Linux / VE6: Hermesサンドボックス |
| ネットワーク | RTX830直結。VLANアウェアブリッジでVLAN20(サーバー)/VLAN25(Hermes)を自己タグ付け。IPは静的設定 |
| バックアップ | Node2 (PBS, VLAN20, 6TB USB HDD) へTier3バックアップ |
| 管理経路 | WireGuard VPN経由のみ。インターネットへ管理画面を公開しない |
| プロジェクト文書 | `plan.md` が設計・決定事項の正本。矛盾があれば作業前に指摘する |

---

## 1. 権限の4段階ルール

### レベルA: 承認不要 (自由に実行してよい)
読み取り専用・状態確認系の操作。

- `qm list` / `pct list` / `qm config` / `pvesm status` / `pveversion`
- `zpool status` / `zfs list` / `zpool list` / `arc_summary`
- `lspci` / `lsusb` / `lsblk` / `ls-iommu` 系 / `dmesg` / `journalctl` / `cat` / `less` / `grep`
- `nvidia-smi` / `ip a` / `ip link` / `bridge vlan` / `ss` / `ping`
- `smartctl -a` (読み取りのみ、`-t` テスト実行は含まない)
- `/etc/pve/` 以下・設定ファイルの閲覧
- 一時ディレクトリ (`/tmp`, `/root/work`) 内でのファイル作成・編集

### レベルB1: 説明して実行してよい (事後報告)
状態を変更するが、**失敗しても数分〜十数分で元に戻せる**操作。
「これから何をするか」を1〜2行で述べてから実行し、結果を報告する。承認待ちで止まらない。

- VM/LXCの起動・停止・再起動 (`qm start/stop/shutdown`, `pct` 同等)
- 新規VM/LXCの作成 (`qm create` / `pct create`) — まだデータが無い段階のもの
- 停止中VMの設定変更 (`qm set`)、`/etc/pve/qemu-server/*.conf` の編集
- `apt update` / 単体パッケージの `apt install`
- ZFSデータセット・スナップショットの**作成** (`zfs create` / `zfs snapshot`)
- systemdサービスの有効化・起動・再起動
- ゲストOS内部での設定作業 (VM内のパッケージ導入、Docker Compose編集など)
- git commit / branch 作成 (§5参照)

### レベルB2: 説明＋承認を得てから実行
可逆ではあるが、**やり直しに数時間〜半日以上、または再ダウンロード・再構築を要する**操作。
手戻り工数の見積もりを添えて承認を求める。

- 稼働中でデータを持つVMの構成変更 (VE1のディスク構成、VE2のプール設定など)
- ストレージプールの追加・削除 (`pvesm add` / `remove`)、ディスクのアタッチ
- ISOやテンプレートの大量ダウンロード、大容量データの移動・コピー
- ファイアウォール設定 (Proxmox firewall) の変更 — 疎通断のリスクがあるため
- カーネルモジュール設定 (`/etc/modules`, `/etc/modprobe.d/`) の編集 ※reboot反映はレベルC
- バックアップジョブの設定変更・削除
- 30分以上かかる処理の開始 (大規模なリストア、`zfs send/recv` など)

**判断基準**: 迷ったら「失敗した時に自分が何をやり直すことになるか」を考える。
コマンド1本で戻せるならB1、手順を踏み直す必要があるならB2、戻せないならC。

### レベルC: 必ず停止して承認を待つ。承認前の実行は禁止
不可逆、またはロックアウト・データ喪失のリスクがある操作。**代替案とロールバック手順(不可能な場合はその旨)を提示してから承認を求めること。**

**ストレージ破壊系 (最重要)**
- `zpool destroy` / `zfs destroy` / `zpool labelclear`
- `wipefs` / `dd` (ブロックデバイス宛) / `mkfs.*` / `sgdisk --zap` / `parted` での削除・再作成
- `qm destroy` / `pct destroy` / ディスクのデタッチ・削除
- `rm -rf` (一時ディレクトリ外)
- LVM削除系 (`lvremove` / `vgremove` / `pvremove`)
- **実行前に必ず対象デバイスを `lsblk` / `zpool status` で再確認し、6TB HDD (実データ) と取り違えていないことを宣言する**

**ロックアウトリスク系**
- `/etc/network/interfaces` の編集と `ifreload -a` (VLANアウェアブリッジ設定ミス = SSH到達不能)
  - 編集時は必ず変更前ファイルを `/root/backup/` にタイムスタンプ付きで退避
  - 適用は「物理コンソール(モニタ+キーボード)にアクセス可能か」を先に確認してから
- SSH設定 (`/etc/ssh/sshd_config`) の変更
- GRUB / カーネルパラメータ変更 (`amd_iommu=on`, `pcie_acs_override` 等) と `update-grub` + reboot
- `vfio-pci` へのデバイスバインド変更 (GPUやSATAコントローラをホストから切り離す操作)

**システム全体系**
- ホストの `reboot` / `shutdown`
- `apt full-upgrade` / `apt dist-upgrade` / Proxmoxメジャーバージョンアップ
- クラスタ操作 (`pvecm` 系)
- ブートローダー・rpool構造に触れる操作

### 承認の粒度 (レベルB2・C共通)

**原則: 「作業単位」でまとめて承認を求める。1コマンドごとに刻まない。**
ただし以下の条件をすべて満たす場合に限る。

- 提示時点で**実行するコマンド全文を列挙**できている (「状況を見ながら判断」は不可)
- まとめた一連が**単一の目的**を持つ (例: 「GRUBにIOMMUパラメータを追加してreboot」は1単位)
- 途中で分岐や判断が発生しない

**次のいずれかに該当する場合は、コマンド単位で個別承認を取り直す。**

- 前のコマンドの出力を見てから次を決める必要がある場合
- 一連の中にレベルCの破壊的操作 (`zpool destroy` / `wipefs` / `mkfs` / `dd`) が含まれる場合
  → **これらは常に単独承認。他の操作と束ねてはならない**
- 実行中にエラーや想定外の出力が出た場合
  → その時点で停止し、当初の承認は失効したものとして扱う。続行の可否を改めて問う

**承認の有効範囲**
- 承認は**その提示内容にのみ有効**。コマンドを1文字でも変更したら再承認が必要
- 承認は**セッションを跨いで持ち越さない**。前回の承認を根拠に今回実行してよいと判断しない
- 「以後この種の操作は承認不要」という包括承認をユーザーが与えた場合は、その旨をCLAUDE.mdへ追記提案する (口頭の合意をファイルに残す)

---

## 2. 作業の原則

1. **スナップショット・ファースト**: VM設定やゲスト内の重要変更の前に、可能なら `zfs snapshot` または `qm snapshot` を取る(レベルB1のため承認不要、そのまま実行してよい)。
2. **1変更→1確認**: 複数の変更をまとめて適用しない。特にネットワークとパススルー関連は、1つ変更するごとに疎通・動作を確認してから次へ進む。
3. **バックアップ確認**: レベルCのストレージ操作の前は、PBS (Node2) 側に有効なバックアップが存在するかを可能な範囲で確認し、結果を報告する。
4. **推測で叩かない**: デバイスパス (`/dev/sdX`) は再起動で変わりうる。破壊的操作では必ず `/dev/disk/by-id/` を使い、シリアル番号で対象を特定する。
5. **plan.md との同期**: アーキテクチャに関わる決定・変更を行ったら、plan.md への反映内容を提案する(勝手に書き換えず差分を提示)。詳細は §5 参照。
6. **わからない時は止まる**: IOMMUグループが想定と違う、エラーの原因が特定できない等の場合、試行錯誤で破壊的コマンドを重ねるのではなく、状況を整理して報告する。

---

## 3. このホスト特有の注意点

- **ネットワークの自己タグ付け構成**: Node0はRTX830に直結し、vmbr0 (VLANアウェア) で管理IF自身がVLAN20タグを付ける。この設定を壊すと管理アクセスが全滅する。ネットワーク変更は物理コンソールを確保できるタイミングでのみ実施。
- **IOMMU作業は段階的に**: BIOS設定→インストール→`for d in /sys/kernel/iommu_groups/*/devices/*; do ...` でグループ確認→パススルー方式決定、の順序を守る。グループが粗い場合の `pcie_acs_override` 追加はレベルC。
- **6TB HDDは「実データの器」**: パススルー方式の検証中も、このディスクへの書き込み・初期化系操作は最大限の警戒対象。
- **GTX1650はVE1専用**: ホスト側でGPUを掴むドライバ(nouveau等)のブラックリスト設定はパススルー準備の一部。変更はレベルC(reboot必要のため)。
- **Node2 (PBS) は別マシン**: Node0上のClaude CodeからNode2へSSHする場合も、本ルールを同様に適用する。

---

## 4. 変更管理 (Git)

構成ファイルはすべてGit管理下に置く。**これはロールバックの主要な手段であり、レベルB/Cの前提条件でもある。**

### リポジトリ構成

```
/root/homelab/            ← 作業ルート。ここでClaude Codeを起動する
├── CLAUDE.md             ← 本ファイル
├── plan.md               ← 設計・決定事項の正本
├── configs/
│   ├── network/          ← /etc/network/interfaces のコピー
│   ├── qemu-server/      ← /etc/pve/qemu-server/*.conf のコピー
│   ├── lxc/              ← /etc/pve/lxc/*.conf のコピー
│   ├── modprobe/         ← vfio-pci, blacklist 設定のコピー
│   └── rtx830/           ← ルーター config のテキスト退避
├── scripts/              ← 運用スクリプト (NUT連携、スナップショット等)
└── docs/                 ← IOMMUグループの記録、トラブル対応ログ
```

### 運用ルール

1. **`/etc/pve/` 内で直接 `git init` しない。** pmxcfs (FUSE + SQLite) 上のため通常のファイルシステムとして扱えない。必ず `configs/` へコピーして管理する。
2. **変更前にコミット**: レベルB2およびレベルCの操作を行う前に、現状のconfigを `configs/` に同期してコミットする。「動いていた状態」をコミットハッシュとして固定してから壊しに行く。
3. **変更後にコミット**: 動作確認が取れた時点で、変更内容と検証結果をコミットメッセージに残す。
4. **コミットメッセージ形式**:
   ```
   <対象>: <変更内容>

   理由: なぜこの変更が必要か
   検証: どう動作確認したか
   戻し方: 元に戻す手順 (該当する場合)
   ```
5. **`/etc` 全体は etckeeper で別途追跡**することを推奨 (`apt install etckeeper`)。apt操作のたびに自動コミットされ、「何を入れたら壊れたか」が追える。導入自体はレベルB1。
6. **秘密情報はコミットしない**: WireGuard秘密鍵、APIトークン、パスワードは `.gitignore` で除外する。誤ってコミットした場合は即座に報告すること (履歴からの除去が必要になる)。
7. **リモートリポジトリ**: 当面はローカルのみで可。GitHub等へpushする場合はプライベートリポジトリとし、6の秘密情報チェックを二重に行う。ネットワーク構成やIPアドレス体系も公開すべき情報ではない。

### Gitで守れないもの
Gitは**テキスト構成の履歴**であって、バックアップではない。VMディスクイメージ・写真ライブラリ・録画データは対象外であり、それらはPBS (Node2) とZFSスナップショットが担当する。混同しないこと。

---

## 5. セッション運用と引き継ぎ

**Claude Codeは前回セッションの記憶を持たない。** 引き継ぎは完全にファイルに依存する。
記録を怠ると、次回セッションは「なぜこの設定になっているのか分からないまま触る」状態から始まり、これが最も事故を招きやすい。

### セッション開始時 (必須)

1. `plan.md` を読む — 設計思想・確定事項・保留事項の**正本**。ここに書かれた決定を、その場の判断で覆さない
2. `docs/worklog.md` の**冒頭「現在の状態」ブロック**と直近2〜3エントリを読む — 前回どこで終わったか、未解決の問題は何か (全文は読まない。§コンテキスト消費の管理 参照)
3. `git log --oneline -20` — 直近の変更履歴
4. **現物と記録の突き合わせ**: `plan.md` の記述と実機の状態が食い違っていないか、関連する範囲だけ確認する
   - 食い違いを見つけたら、**推測で修正せず、まず報告する**。実機が正しいのか記録が正しいのかはユーザーにしか判断できない

### セッション終了時 (必須)

`docs/worklog.md` に追記する。形式:

```markdown
## YYYY-MM-DD

### やったこと
- 実施内容と結果 (成功/失敗の両方)

### 決めたこと
- 選択した方式と、その理由。**採用しなかった案とその却下理由も書く**
  (これが無いと、次回同じ検討を最初からやり直すことになる)

### 未解決・次回やること
- 詰まっている点、次の一手

### 実機の状態
- 現在稼働しているもの / 停止中のもの / 中途半端な状態のもの
```

### コンテキスト消費の管理

**引き継ぎ資料は増え続ける。読む量を制御しないと、作業そのものに使える容量を圧迫する。**

**worklog.md の構造** — 冒頭に固定サイズの要約ブロックを置き、その下に時系列エントリを**新しい順**で並べる。

```markdown
# 作業履歴

## 現在の状態 (毎セッション末尾に上書き更新 / 20行以内)
- 稼働中: (VM/サービス)
- 中途半端な状態: (作業途中で止まっているもの)
- 次の一手: (最大3件)
- 注意中の問題: (最大3件)

---
## 2026-08-05
(最新エントリ)
## 2026-08-01
(以下、過去)
```

**読む量のルール**

| 対象 | 読み方 |
|---|---|
| worklog.md の「現在の状態」 | 毎回必ず読む |
| worklog.md の過去エントリ | 直近2〜3件のみ。それ以前は**必要になった時に検索して読む** |
| plan.md | 全文を読むのは方針確認が必要な時のみ。通常は該当セクションのみ |
| docs/ の各記録 | 該当作業に関係するものだけ |

**ローテーション**
- worklog.md が **500行を超えたら** `docs/archive/worklog-YYYY-QN.md` へ古いエントリを移す (レベルB1)
- アーカイブは消さない。`grep` で検索可能な状態を保てば、読み込まなくても参照できる

**コマンド出力によるコンテキスト圧迫の回避**
記録より、実はこちらの方が消費が大きい。

- `dmesg` / `journalctl` は**全文を出さない**。`grep` / `tail -n 50` / `--since` で絞る
- 長い出力は「全文を読む」のではなく、必要な行だけ抽出して報告する
- 大きなファイルは `cat` せず、`grep -n` で該当箇所を特定してから範囲指定で読む
- 出力が数百行に及ぶコマンドは、まず件数だけ確認 (`| wc -l`) してから絞り込む

**セッションを分ける判断**
1セッションで扱う作業は、原則として **plan.md の「次のステップ」1項目分**に留める。
「BIOS設定→インストール→IOMMU確認→パススルー決定」を1セッションで通そうとすると、後半で判断材料を見失う。区切りごとに worklog を書いてセッションを閉じる方が、結果的に速い。

---

### plan.md の位置づけ

| | plan.md | docs/worklog.md |
|---|---|---|
| 内容 | **現在の設計・確定事項** (常に最新の姿) | **時系列の作業履歴** (追記のみ、過去は消さない) |
| 更新 | アーキテクチャ決定が変わった時 | 毎セッション |
| 扱い | 上書き更新。古い記述は消す | 追記専用 |

- **plan.md は勝手に書き換えない。** 変更が必要な場合は差分を提示し、承認を得てから反映する (レベルB1相当だが、設計文書は例外的に承認を要する)
  - **包括承認 (2026-08-04)**: 「実績は都度報告の上自主的に更新してよい」とユーザーから包括承認を得た。以後、plan.md の更新は**都度の事前承認を待たず**、部分置換で反映したうえで**その回答の中で更新内容を報告する**運用とする。承認自体の取り消し・範囲変更があれば本項を更新する
- 編集は**大規模な書き直しではなく、変更点ごとの部分置換**で行う。ファイル全体を再生成すると、記録されていた細部が静かに失われる
- **ファイル上の plan.md が会話の内容より古い可能性がある。** 開始時に必ず現物を読み、記憶や推測で内容を補わない

### 記録すべき「実測値」

このプロジェクトは「実測・検証を経てから確定させる」方針のため、以下は必ず `docs/` に残す。

- IOMMUグループの一覧 (`docs/iommu-groups.md`) — パススルー方式判断の根拠
- ディスクのシリアル番号と役割の対応表 (`docs/disks.md`) — **破壊的操作時の取り違え防止に直結**
- VRAM使用量の実測記録 — Frigate/Immich同居の妥当性検証
- ネットワーク疎通テストの結果 — VLAN間フィルターの検証記録
- トラブルとその解決手順 — 同じ問題を二度調べ直さないため

---

## 6. 禁止事項 (承認があっても行わない)

- 管理画面 (8006) やSSHをインターネットへ直接公開する設定
- WireGuard以外の外部アクセス経路の追加 (Cloudflare Tunnel は Immich 専用LXCのみ、plan.md の方針に従う)
- rpool (OS領域) 上での実験的操作
- ファイアウォール・フィルターの全開放 (「一時的にでも全部通して試す」は不可。必要最小限のピンホールで検証する)
- 認証情報 (パスワード、APIキー、WireGuard秘密鍵) の平文出力・ログへの書き出し
