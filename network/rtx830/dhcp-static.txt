# MACアドレス固定IP割り当て一覧
#
# このファイルの内容を RTX830 に貼り付けて実行すると固定IP割り当てが追加されます。
# デバイスを追加したら dhcp scope bind の行を追記して save で保存してください。
#
# 【IPアドレスの使い分け】
#   192.168.1.  2 〜 191 : DHCP 動的配布（新規デバイス自動取得）
#   192.168.1.192 〜 254 : 固定割り当て推奨ゾーン（ここで管理）
#
# 【MACアドレスの確認方法】
#   Windows : コマンドプロンプトで  ipconfig /all  → 「物理アドレス」
#   macOS   : ターミナルで          ifconfig en0   → 「ether」
#   Linux   : ターミナルで          ip link        → 「link/ether」
#   Android : 設定 → Wi-Fi → 対象ネットワーク → 詳細 → MACアドレス
#   iOS     : 設定 → Wi-Fi → (i) → MACアドレス（プライベートMACがOFFの場合）
# -----------------------------------------------------------------------

# -----------------------------------------------------------------------
# 固定IP割り当て設定
#   ★ MACアドレスとIPアドレスを実際のデバイスに合わせて書き換えてください
# -----------------------------------------------------------------------

# dhcp scope bind 1 192.168.1.192 ethernet xx:xx:xx:xx:xx:xx  # デバイス名（例: NAS）
# dhcp scope bind 1 192.168.1.193 ethernet xx:xx:xx:xx:xx:xx  # デバイス名（例: プリンター）
# dhcp scope bind 1 192.168.1.194 ethernet xx:xx:xx:xx:xx:xx  # デバイス名（例: スマートTV）

# -----------------------------------------------------------------------
# 設定確認コマンド（適用後に実行して確認）
# -----------------------------------------------------------------------
# show dhcp scope
# show dhcp binding

# -----------------------------------------------------------------------
# 適用後に保存
# -----------------------------------------------------------------------
# save
